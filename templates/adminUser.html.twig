{% extends "admin.html.twig" %}

{% block adminContent %}
    <div class="container container--admin">
        <section class="section">
            <h1>{{ t('admin.users.titles.edit')|trans }}</h1>
            {{ form(form, {'attr': {'class': 'adminForm'}}) }}
        </section>
    </div>
{% endblock %}