<!DOCTYPE html>
<html lang="pl-PL">
    {{ include('head.html.twig') }}
    <body>
    <header>
        {% block menu %}
                {{ render(controller('App\\Controller\\MenuController:menu')) }}
        {% endblock %}
    </header>
    <main>
        {% block main %}
        <div class = "top">
            
        </div>
            <div>
                {{ t('home.welcome')|trans }}
            </div>
        {% endblock %}
    </main>
    </body>
</html>
